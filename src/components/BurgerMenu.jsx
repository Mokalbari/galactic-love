import './Header.css'

function BurgerMenu({ filter }) {
  return (
    <nav className="burger-menu">
      {/* <i class="bx bx-user" type="button" onClick={() => filter()} />
      <i class="bx bx-user" type="button" onClick={() => filter()} />
      <i class="bx bx-user" type="button" onClick={() => filter()}></i>
      <i class="bx bx-user" type="button" onClick={() => filter()}></i>
      <i class="bx bx-user" type="button" onClick={() => filter()}></i> */}
      {/* <button type="button" onClick={() =>filter() }>age</button>
            <button type="button" onClick={() =>filter() }>race</button>
            <button type="button" onClick={() =>filter() }>affiliation</button>
            <button type="button" onClick={() =>filter() }>genre</button>
            <button type="button" onClick={() =>filter() }>planete</button> */}
    </nav>
  )
}

export default BurgerMenu
